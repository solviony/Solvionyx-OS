diff --git a/build_iso_debian.sh b/build_iso_debian.sh
index 1234567..abcdef0 100755
--- a/build_iso_debian.sh
+++ b/build_iso_debian.sh
@@ -415,11 +415,20 @@ echo "🔧 Installing ImageMagick..."
 apt-get install -y -qq imagemagick

 echo "🎨 Ensuring Solvionyx logo exists..."
-if [ ! -f "$LOGO_FILE" ]; then
-  echo "⚠️ Missing ${LOGO_FILE} — generating fallback Solvionyx logo..."
-  convert -size 512x128 xc:'#0b1220' -gravity center \
-    -fill '#6f3bff' -pointsize 40 -annotate 0 'SOLVIONYX OS' "$LOGO_FILE"
-fi
+
+# Validate logo integrity — replace only if missing OR corrupt
+if ! identify "$LOGO_FILE" >/dev/null 2>&1; then
+  echo "⚠️ ${LOGO_FILE} missing or invalid — generating fallback Solvionyx logo..."
+  convert -size 512x128 xc:'#0b1220' -gravity center \
+    -fill '#6f3bff' -pointsize 40 -annotate 0 'SOLVIONYX OS' "$LOGO_FILE"
+fi


@@ -1025,11 +1034,11 @@ case "${EDITION}" in
     ;;
   kde)
     apt-get install -y -qq \
-      task-kde-desktop sddm konsole \
-      python3-gi gir1.2-gtk-3.0 \
-      ubiquity
+      task-kde-desktop sddm konsole \
+      python3-gi gir1.2-gtk-3.0 \
+      calamares
     ;;
 esac
